# NeoReef Cesium Pipeline dependencies
# On Windows, install GDAL-based packages via conda:
#   conda install -c conda-forge rasterio geopandas fiona
# Then install the rest via pip:
#   pip install requests boto3

rasterio>=1.3
geopandas>=0.13
fiona>=1.9
requests>=2.28
boto3>=1.26
